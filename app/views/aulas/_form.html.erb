<%= form_with(model: aula, local: true) do |form| %>
  <% if aula.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(aula.errors.count, "error") %> prohibited this aula from being saved:</h2>

      <ul>
      <% aula.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <table class="table table-striped">

  <tbody>
    <tr>

            <td>  <div class="field">
                <%= form.label :nombre %>
                <%= form.text_field :nombre, id: :aula_nombre %>
              </div>
            </td>


            <td>  <div class="field">
                <%= form.label :bloque %>
                <%= form.text_field :bloque, id: :aula_bloque %>
              </div>

            </td>

            <td><div class="field">
                <%= form.label :capacidad %>
                <%= form.text_field :capacidad, id: :aula_capacidad %>
              </div>
            </td>  

            <td><div class="field">
                <%= form.label :observacion %>
                <%= form.text_field :observacion, id: :aula_observacion %>
              </div>
            </td>
          </div>
          <td><div class="actions">
              <%= form.submit %>
            </div>
          </td>
        <% end %>
    </tr>
  </tbody>
</table>