<%= form_with(model: asignacion_aula, local: true) do |form| %>
  

  <% if asignacion_aula.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(asignacion_aula.errors.count, "error") %> prohibited this asignacion_aula from being saved:</h2>

      <ul>
      <% asignacion_aula.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<table class="table table-striped">

  <tbody>
    <tr>
        <td><div class="field">
            <%= form.label :facultads_id %><br>
            <%= form.collection_select(:facultad_id, Facultad.all, :id, :nombre) %><br></div></td>

        <td><div class="field">
              <%= form.label :registro_carrera_id %><br>
              <%= form.collection_select(:registro_carrera_id, RegistroCarrera.all, :id, :nombreCarrera) %><br></div></td>

           <td><div class="field">
              <%= form.label :anho_carrera_id %><br></div></td>

           <td><div class="field">
              <%= form.collection_select(:anho_carrera_id, AnhoCarrera.all, :id, :anho) %><br></div></td>

          
           <td><div class="field">
              <%= form.label :aulas_id %><br>
              <%= form.collection_select(:aula_id, Aula.all, :id, :nombre) %><br></div></td>
               <td><div class="field">
              <%= form.label :observacion %>
              <%= form.text_field :observacion, id: :asignacion_aula_observacion %>
        </div></td>

  <td><div class="actions">
      <%= form.submit %>
    </div>
  </td>
<% end %>
</tr>
</tbody>
</table>

