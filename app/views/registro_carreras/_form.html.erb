 <%= form_with(model: registro_carrera, local: true) do |form| %>
  <% if registro_carrera.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(registro_carrera.errors.count, "error") %> prohibited this registro_carrera from being saved:</h2>

      <ul>
      <% registro_carrera.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="container">
<div class="row">
  <div class="span8">

    <table class="table table-striped">

      <tbody>
        <tr>

          <td>  <div class="field">
              <%= form.label :Carrera %>
              <%= form.text_field :nombreCarrera, id: :registro_carrera_nombreCarrera %>
            </div>
          </td>

          <td>
            <div class="actions">
              <%= form.submit %>
            </div>
          </td>  
          <% end %>
      <tr>
      </tbody>
    </table>  
  </div>
</div>
</div>